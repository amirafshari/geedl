job_name: sentinel1_test

roi:
  path: shahrestan/shahrestan.shp

dataset:
  name: sentinel-1
  bands:
    select: [VV, VH]

date:
  start: "2022-04-22"
  end: "2022-06-22"

composite:
  strategy: mean
  window:
    type: fixed_days
    size: 30
    step: 30
    label_format: "%Y-%m-%d"

output:
  dir: ./output
  format: COG
  dtype: float32

asset:
  project: ee-tat3
  base_path: projects/ee-tat3/assets

auth:
  method: service_account          # or service_account or browser
  # required only for service_account:
  service_account_email: amir-840@ee-tat3.iam.gserviceaccount.com
  key_file: ee-tat3-835f3bd207eb.json